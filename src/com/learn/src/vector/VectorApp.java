package com.learn.src.vector;

import java.util.Iterator;
import java.util.Vector;

public class VectorApp {
    public static void main(String[] args) {
        Vector vector = new Vector();
        vector.add(2);
        vector.remove(0);
        Iterator iterator = vector.iterator();

    }
}
