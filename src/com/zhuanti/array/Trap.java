package com.zhuanti.array;

/**
 * 42  Trap
 *
 * 这个和之前的那个有一些不一样
 *
 */
public class Trap {
    /**
     * 思路：
     *
     * 计算出高度，一层一层的计算
     * @param height
     * @return
     */
    public int trap(int[] height) {
        int maxHight = height.length;
        for (int i = 0; i < maxHight; i++) {

        }
        return 0;
    }
}
