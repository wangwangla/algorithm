package com.tophund;

public class KuoHaoisValid {
    /**
     * 遇到左边的，就直接进入栈中
     *
     * 遇到右边的就去判断前一个是不是左边，不是，就入栈
     * @param s
     * @return
     */
    public boolean isValid(String s) {

        return false;
    }
}
